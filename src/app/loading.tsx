import { lo } from '@/lib/lao';

export default function Loading() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p>{lo.common.loading}</p>
    </main>
  )
}


